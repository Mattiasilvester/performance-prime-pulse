# Copia come .env e compila i valori.
# Per redirect Stripe/Supabase auth in Capacitor serve l'URL di produzione.

# App URL (per redirect return_url, emailRedirectTo, redirectTo)
# Su web può essere omesso (si usa window.location.origin). In Capacitor impostare alla produzione.
VITE_APP_URL=https://pro.performanceprime.it
