{
  "name": "performance-prime",
  "private": true,
  "scripts": {
    "dev:user": "pnpm --filter @pp/app-user dev",
    "dev:pro": "pnpm --filter @pp/app-pro dev",
    "build:user": "pnpm --filter @pp/app-user build",
    "build:pro": "pnpm --filter @pp/app-pro build",
    "build:all": "pnpm -r build",
    "lint:all": "pnpm -r lint",
    "type-check:all": "pnpm -r type-check"
  },
  "devDependencies": {
    "typescript": "^5.4.0"
  },
  "engines": {
    "node": ">=18",
    "pnpm": ">=9"
  }
}
